device: &device 'cuda'
seed: &seed 0
bounds_min: &bounds_min [-0.3, -0.7, 0]
bounds_max: &bounds_max [0.3, -0.3, 0.45]

keypoint_proposer:
  num_candidates_per_mask: 5
  min_dist_bt_keypoints: 0.06
  max_mask_ratio: 0.5
  device: *device
  bounds_min: *bounds_min
  bounds_max: *bounds_max
  seed: *seed
  handeye_translation: [-0.688436053, -0.511152620, 1.247442]
  handeye_quaternion: [0.84561027, -0.0208527, -0.53332, 0.00861711]  # [x, y, z, w]
  save_dir: '/home/zhuoli/xtrainer_clover/assets/scene_observation/'